language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: nosetests --with-cov --cov-report html --cov-config .coveragerc  --cov buffet tests/
after_success:
    - coveralls